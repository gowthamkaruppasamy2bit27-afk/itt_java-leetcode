bool isIsomorphic(char* s, char* t) {
    int i,j,l,l1;
    l=strlen(s);
    l1=strlen(t);
    if(l!=l1)
    return false;
    for(i=0;i<l;i++)
    for(j=i+1;j<l;j++)
        if(((s[i]==s[j]) && (t[i]!=t[j]))||((s[i]!=s[j])&&(t[i]==t[j])))
        return false;
    return true; 

}
